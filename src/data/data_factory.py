"""Create datasets.Load labels and features/1s time series from disk and create the actual dataset. """

import os
from src.utils.constants import TEAM_NAMES, TEAMWORK_SESSION_DAYS


def map_emotions_to_signal(matches) -> None:
    """
    Load labels and features/1s time series from disk and create the actual dataset.
    """
    print("Hi.")


















if __name__ == "__main__":
    for team in TEAM_NAMES:
        for day in TEAMWORK_SESSION_DAYS:
            pass